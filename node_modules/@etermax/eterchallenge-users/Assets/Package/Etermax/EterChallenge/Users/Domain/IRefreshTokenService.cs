using System;

namespace Package.Etermax.EterChallenge.Users.Domain
{
    public interface IRefreshTokenService
    {
        IObservable<Credentials> Refresh(string userId, string refreshToken);
    }
}