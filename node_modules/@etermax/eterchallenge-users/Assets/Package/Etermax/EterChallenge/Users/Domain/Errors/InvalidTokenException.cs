using System;

namespace Package.Etermax.EterChallenge.Users.Domain.Errors
{
    public class InvalidTokenException : Exception
    {
        public InvalidTokenException(string accessToken, string refreshToken)
            : base("Token values cannot be null or empty: accessToken " + accessToken + " refreshToken " + refreshToken)
        {
        }
    }
}