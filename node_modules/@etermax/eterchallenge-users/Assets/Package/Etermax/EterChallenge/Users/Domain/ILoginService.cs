using System;
using System.Collections.Generic;

namespace Package.Etermax.EterChallenge.Users.Domain
{
    public interface ILoginService
    {
        IObservable<User> Execute<T>(string userId, string provider, string providerId, T credentials,
            Dictionary<string, object> userMetadata = null);
    }
}