using System;
using UniRx;

namespace Etermax.Web.Client
{
    public interface IResponseProxy
    {
        IObservable<HttpWebResponse> Proxy(IHttpWebRequest request, HttpWebResponse response);
    }
}