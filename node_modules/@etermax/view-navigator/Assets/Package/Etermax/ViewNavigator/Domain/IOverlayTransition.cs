using System;
using UniRx;
using UnityEngine;

namespace Etermax.ViewNavigator.Domain 
{
    public interface IOverlayTransition 
    {
        void In(GameObject @from, GameObject to, Action onStart, Action onComplete);
        void Out(GameObject currentView, GameObject targetView, Action onStart, Action onComplete);
        IObservable<Unit> FadeInBackground(string viewGuid, Transform parentTransform);
        IObservable<Unit> FadeOutBackground();
    }
}